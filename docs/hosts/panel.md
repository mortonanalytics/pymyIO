# Panel (HoloViz)

**Tier 2** — documented best-effort, verified manually before each release.

## Render idiom

- `pn.pane.IPyWidget(MyIO(...).render())`

## Notes

- anywidget instances are ipywidgets.
- No conversion required.
