# Classic Jupyter Notebook

**Tier 2** — documented best-effort, verified manually before each release.

## Compatibility

- Notebook 7.x works.
- Notebook 7 shares the JupyterLab widget protocol.
- Notebook < 7 requires the anywidget ESM shim.
- Notebook < 7 is not officially supported; users should upgrade.
